# Playtime statusline config.
#
# INNER_STATUSLINE is your *base* statusline command. The playtime wrapper runs
# it, then appends the "⏱ hours played" segment. To switch statuslines, change
# this ONE line — the playtime tracker keeps working regardless.
#
# It receives Claude Code's statusline JSON on stdin, same as any statusline cmd.
# Set it to '' (empty) to show ONLY the playtime tracker.

INNER_STATUSLINE='/home/kevin/.nvm/versions/node/v22.6.0/bin/node /home/kevin/.claude/hooks/gsd-statusline.js'

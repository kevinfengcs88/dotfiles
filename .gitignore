# zsh compdump caches (generated per-host, never commit)
.zcompdump*

# stray tmux sockets
.tmux-*.sock

# stray Claude Code session transcripts
STOW.md
*.session.md

# playtime runtime session data (per-host, not portable)
home/.claude/playtime/data/sessions-*.jsonl

# python bytecode
__pycache__/
*.pyc
